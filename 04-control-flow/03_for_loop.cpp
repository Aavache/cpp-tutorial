#include <iostream>

int main(){
    // For loop are ideal to run for a fixed number of iterations
    // The syntax is: for (initialization; condition; increment)
    for (int i = 0; i < 10; ++i){
        std::cout << "Iteration " << i << std::endl;
    }    
}